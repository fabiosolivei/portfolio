# fabiosolivei.github.io

Personal portfolio site — AI & Enterprise Product Leader.

Built with HTML + Tailwind CSS (CDN). No build step required.

## Local Development

Open `index.html` directly in a browser, or serve locally:

```bash
npx serve .
```

## Deployment

Deployed automatically via GitHub Pages from the `main` branch.
